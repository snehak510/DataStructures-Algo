Design Google calender

Functional requirements

1. create events
   a. can be recurring
2. update events
3. Users should be able to list all events
4. Should be able to delete event
5. should be able to get Information about specifc events
\
----------------------------------------------------

API Endpoints

user entity
event entity

EVent Microservice (C,R,U,D apis for events)
Users microservice would mamage user entity

1. Events create HTTP POST /events
2. Events update HTTP POST  /evemts/{event_id}
3. Events list HTTP POST    /events
4. Events delete HTTP POST  /evemts/{event_id}
5. Events getInfo HTTP POST   /evemts/{event_id}



--------------------------------------------------------
Data modellin

Events, users, events_users

events
id
start_time
end_time
type
is_recurring
recurrence_rule
create_time
last_updates
host


events_users
1. eventid
2. user id
3. create_time
4. last_updated_time


users
id
fname
lname
email
create_time
last_updated_time


--------------------------------------------------------------


client ->  Web server -> event micro services       -> datat base
                      -> users microservice
                      --> id generation microservice

